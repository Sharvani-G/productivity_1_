<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>My Notes</title>
  <link rel="stylesheet" href="/notes_app/styles/layout.css">
  <link rel="stylesheet" href="/notes_app/styles/themes.css">
  <link rel="stylesheet" href="/notes_app/styles/notes.css">
</head>
<body>
  <!-- Back Button -->
  <a href="/" style="position: fixed; top: 20px; left: 20px; z-index: 9999; display: flex; align-items: center; justify-content: center; width: 50px; height: 50px; background-color: #fff; border-radius: 50%; transition: 0.2s; box-shadow: 0 2px 8px rgba(0,0,0,0.2);">
    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#000" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
      <polyline points="15 18 9 12 15 6"></polyline>
    </svg>
  </a>
  
  <main class="app">
    <header class="app-header">
      <h1>📝 My Notes</h1>
      <nav class="header-actions">
        <button id="themeToggle" class="btn">Theme</button>
      </nav>
    </header>

    <section class="composer">
      <textarea id="noteInput" placeholder="Write a note..." aria-label="note-input"></textarea>
      <div class="composer-row">
        <select id="categorySelect" aria-label="note-category">
          <option value="General">General</option>
          <option value="Study">Study</option>
          <option value="Work">Work</option>
          <option value="Personal">Personal</option>
          <option value="Ideas">Ideas</option>
          <option value="Todo">Todo</option>
        </select>
        <label><input type="checkbox" id="insertTop"> Insert top</label>
        <button id="addBtn" class="btn">Add</button>
      </div>
      <div class="composer-row">
        <button id="downloadBtn" class="btn ghost">Download This</button>
      </div>
      <div class="composer-hint">✨ Color-coded boxes • Pin favorites • Organize by category • Track todos • Light/Dark/Black themes</div>
    </section>

    <section class="controls">
      <input id="search" placeholder="Search notes by text..." />
      <select id="filterCategory" aria-label="filter-category">
        <option value="">All Categories</option>
        <option value="General">General</option>
        <option value="Study">Study</option>
        <option value="Work">Work</option>
        <option value="Personal">Personal</option>
        <option value="Ideas">Ideas</option>
        <option value="Todo">Todo</option>
      </select>
    </section>

    <section id="notesList" class="notes-list" aria-live="polite"></section>
  </main>

  <!-- Edit Modal -->
  <div id="editModal" style="display:none; position:fixed; top:0; left:0; width:100%; height:100%; background:rgba(0,0,0,0.5); z-index:1000; align-items:center; justify-content:center;">
    <div style="background:var(--card, white); padding:20px; border-radius:8px; width:90%; max-width:500px; box-shadow:0 4px 6px rgba(0,0,0,0.3); color:var(--text, black);">
      <h2 style="margin-top:0; color:var(--text, black);">Edit Note</h2>
      <textarea id="editInput" style="width:100%; height:120px; padding:8px; border:1px solid var(--border, #ccc); border-radius:4px; font-family:inherit; font-size:14px; background:var(--bg, #fff); color:var(--text, black); box-sizing:border-box;"></textarea>
      <div style="margin-top:15px; display:flex; gap:10px; justify-content:flex-end;">
        <button id="cancelEdit" style="padding:8px 16px; background:#ccc; border:none; border-radius:4px; cursor:pointer; color:black;">Cancel</button>
        <button id="saveEdit" style="padding:8px 16px; background:#2563eb; color:white; border:none; border-radius:4px; cursor:pointer; font-weight:600;">Save</button>
      </div>
    </div>
  </div>

  <script src="/notes_app/js/api.js" type="module"></script>
  <script src="/notes_app/js/notes_app.js" type="module" nonce="<%= cspNonce %>"></script>
</body>
</html>
